export default function initAdminUsersController(db) {
  const index = async (request, response) => {
    try {
    } catch (error) {
      console.log(error);
    }
  };

  return {
    index,
  };
}
